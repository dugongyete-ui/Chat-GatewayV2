kind = "web"
previewPath = "/"
title = "Qwen API Gateway"
version = "1.0.0"
id = "artifacts/gateway"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24845

[services.development]
run = "pnpm --filter @workspace/gateway run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gateway", "run", "build" ]
publicDir = "artifacts/gateway/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24845"
BASE_PATH = "/"
